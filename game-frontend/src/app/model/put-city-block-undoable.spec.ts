import { PutCityBlockUndoable } from './put-city-block-undoable';

describe('PutCityBlockUndoable', () => {
  it('should create an instance', () => {
    expect(new PutCityBlockUndoable()).toBeTruthy();
  });
});
