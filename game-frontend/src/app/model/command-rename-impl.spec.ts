import { CommandRenameImpl } from './command-rename-impl';

describe('CommandRenameImpl', () => {
  it('should create an instance', () => {
    expect(new CommandRenameImpl()).toBeTruthy();
  });
});
