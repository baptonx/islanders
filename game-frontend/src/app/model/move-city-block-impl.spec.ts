import { MoveCityBlockImpl } from './move-city-block-impl';

describe('MoveCityBlockImpl', () => {
  it('should create an instance', () => {
    expect(new MoveCityBlockImpl()).toBeTruthy();
  });
});
